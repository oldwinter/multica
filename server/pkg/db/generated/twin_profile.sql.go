// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: twin_profile.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getTwinProfileByWorkspace = `-- name: GetTwinProfileByWorkspace :one

SELECT id,
       workspace_id,
       name,
       state,
       review_digest,
       source_count,
       assertion_count,
       skill_count,
       rule_count,
       assertions,
       topics,
       review_steps,
       created_at,
       updated_at
FROM twin_profile
WHERE workspace_id = $1
`

// Twin profile metadata is workspace-scoped. Raw evidence, local artifacts,
// paths, logs, and secrets are intentionally not represented here.
func (q *Queries) GetTwinProfileByWorkspace(ctx context.Context, workspaceID pgtype.UUID) (TwinProfile, error) {
	row := q.db.QueryRow(ctx, getTwinProfileByWorkspace, workspaceID)
	var i TwinProfile
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.Name,
		&i.State,
		&i.ReviewDigest,
		&i.SourceCount,
		&i.AssertionCount,
		&i.SkillCount,
		&i.RuleCount,
		&i.Assertions,
		&i.Topics,
		&i.ReviewSteps,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertSignedTwinProfile = `-- name: UpsertSignedTwinProfile :one
INSERT INTO twin_profile (
    workspace_id,
    name,
    state,
    review_digest,
    review_steps
) VALUES (
    $1,
    'Workspace Twin',
    'signed-off',
    $2,
    $3::jsonb
)
ON CONFLICT (workspace_id) DO UPDATE
SET state = EXCLUDED.state,
    review_digest = EXCLUDED.review_digest,
    review_steps = EXCLUDED.review_steps,
    updated_at = now()
RETURNING id, workspace_id, name, state, review_digest, source_count, assertion_count, skill_count, rule_count, assertions, topics, review_steps, created_at, updated_at
`

type UpsertSignedTwinProfileParams struct {
	WorkspaceID  pgtype.UUID `json:"workspace_id"`
	ReviewDigest string      `json:"review_digest"`
	ReviewSteps  []byte      `json:"review_steps"`
}

func (q *Queries) UpsertSignedTwinProfile(ctx context.Context, arg UpsertSignedTwinProfileParams) (TwinProfile, error) {
	row := q.db.QueryRow(ctx, upsertSignedTwinProfile, arg.WorkspaceID, arg.ReviewDigest, arg.ReviewSteps)
	var i TwinProfile
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.Name,
		&i.State,
		&i.ReviewDigest,
		&i.SourceCount,
		&i.AssertionCount,
		&i.SkillCount,
		&i.RuleCount,
		&i.Assertions,
		&i.Topics,
		&i.ReviewSteps,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
